{% extends '/base.html.twig' %}
{% block title %}{{ 'title'|trans({}, 'common') }}{% endblock %}

{% block body %}
    {% for row in data %}
        <div>{{ row.user.id }} | {{ row.user.name }} | {{ row.address.street }}</div>
    {% endfor %}
{% endblock %}